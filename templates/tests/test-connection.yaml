apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "orihime.fullname" . }}-test-connection"
  labels:
{{ include "orihime.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "orihime.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
